# Human-readable dependency list for the micro-CT analysis pipeline.
#
# For INSTALLATION, prefer the version-locked file, which pins the exact
# versions from a known-working environment:
#
#     pip install -r requirements.lock.txt
#
# This file documents the direct dependencies and why each is needed.
# The environment was built with pip (not conda); use pip on all machines.
#
# Requires Python 3.10+.

# --- Core numerical / imaging ---
numpy==2.2.6                 # note: NumPy 2.x; io.py contains a 2.0 compat shim
scipy
scikit-image                 # provides `skimage`
connected-components-3d      # provides `cc3d`
tifffile

# --- Avizo / Amira file reading ---
ahds

# --- Database ---
duckdb

# --- Reference saturation spreadsheet (.xlsx) ---
openpyxl

# --- Simulation config writing (solve.py) ---
PyYAML

# --- Terminal UI ---
rich

# --- 3D viewer (PyVista) ---
pyvista
pyvistaqt                    # background plotter; pulls in PyQt5 + vtk

# --- Analytics dashboard ---
dash
dash-bootstrap-components
plotly

# --- Plotting / image scripts ---
matplotlib
pillow                       # provides `PIL`

# NOTE: MPLBM-UT and MPI are not pip packages; install them separately on the
# machine that runs the flow simulations (see README).
